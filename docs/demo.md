# Demo script — 2.5 min, 3 scenes + feedback beat

Pre-flight (before recording):
- [ ] `caffeinate -dis` in a spare terminal (the app's backend dies on sleep
      and does not restart itself)
- [ ] `curl -s localhost:8799/api/health` returns JSON (app backend alive)
- [ ] `scripts/reset_demo.sh all` — clears logs, agent artifacts, and the war
      room so cards/briefs/audit entries regenerate ON CAMERA (enrichment
      cache, corrections, and eval reports are never touched)
- [ ] `.venv/bin/python -m pytest` → 58 passed (show briefly if time allows)
- [ ] Terminal font ≥14pt, clean desktop profile, notifications off
- [ ] Browser tab open on `reports/ops-brief-2026-W33.html` (last shot)

## Scene 1 — Live catch (~60s)
```bash
.venv/bin/python -m src.mock_helpdesk --cursor 2026-08-03T00:00 --speed 3600 &
.venv/bin/python -m src.triage --watch --from 2026-08-03T00:00 --poll 0.5
```
Narrate: "56 tickets of history are triaged; now the queue replays Aug 3
onward at one hour per second."
- Beat 1: quiet stream of arrivals → **INCIDENT_DECLARED INC-001 on TKT-2060**
  — "third similar ticket, 7 hours after the first report. The real team
  worked these as 13 separate cases for 3 days."
- Beat 2: **TREND_DECLARED on TKT-2056** — "11 near-identical FX complaints
  over 3 weeks — never more than 2 in one day. No daily review can see this."
- Beat 3: **RISK_PINNED TKT-2079** — show the ticket subject: "Quick
  question." It's an unrecognized admin user — possible account takeover.
- Round trip: `curl -s localhost:8900/tickets/TKT-2072 | jq .labels` — the
  pipeline wrote segment/priority/incident back to the helpdesk.

*Fallback storyboard (if replay is cut):* run `python -m src.triage --all`,
show the queue table with the 4 risk tickets pinned, then open
`out/events.jsonl` from a pre-recorded watch run.

## Scene 2 — The pattern cards + the handoff (~50s)

*War-room variant (primary): shoot this scene in OpenMausBot instead — the
3-kickoff flow in `docs/openmausbot-setup.md` (Triage → Pattern Commander
auto-handoff; you, as incident manager sophie, green-light `@Liaison go
ahead`; Alex/Mei respond in human voice). Then the **closure beat (~15s)**:
per setup doc step 2b you log owner_acked → draft_approved → fix_confirmed →
customers_notified for INC-001 and rerun metrics — the card's closure cell
advances and time-to-close fills in, while INC-002 visibly stays
"cannot close" (its theme is still active). Two lines to say on camera:
"every customer draft stays DRAFT until a human releases it — the send
button is never the AI's" and "the pattern advances toward closed — state
tracking, not fix verification." The file walkthrough below is the
plain-Claude-Code fallback.*
- Open `incidents/INC-001.md`: blast radius by MRR, canonical customer draft,
  membership note ("the classifier flags TKT-2070 as a probable 14th member —
  it never says 'card'").
- Open `knowledge/outbound/eng/INC-001-brief.md`: correlation hypothesis
  against the Aug-4 3DS change — **point at the SIMULATED label on screen**.
  "Filed as OPS-101 in the mock tracker."
- Flash `trends/TRD-001.md`: "$58k MRR — a third of the book — telling us our
  FX pricing lost them. This goes to the pricing owner, not back to support."

## Scene 3 — Work package + coaching (~40s)
- Open `workpackages/TKT-2072.md`: the $118k wire — facts table, ±2% portal
  search window, ready-to-send partner-bank inquiry, customer update deferred
  to the incident's canonical draft.
- **Feedback beat (~20s):** show `feedback/corrections.jsonl` (2 staff
  corrections, signed with roster handles — only ops roles in
  `team/roster.json` can author accepted corrections) → show the eval delta
  side by side: segment **75% → 96%** ("one correction generalized to every
  wire ticket") and ops_workflow 88% → 83% ("and the eval caught a coaching
  side effect — that's why the harness exists").
- Close on the weekly brief in the browser: "$191.5k stuck, who's exposed,
  and what to deflect — generated by a script, because reporting doesn't need
  an agent."

## Optional Act 2 — the four evolution channels (~90s, if time allows)
1. **Correction generalizes (Triage):** post the TKT-2090 correction signed
   `— sophie` → show baseline vs after eval reports side by side (75→96%,
   plus the caught side effect — the harness weighs both directions).
2. **Coaching (PC via Raj):** `@Raj should escalation notes CC compliance?
   If yes, ask the right agent to make it a rule.` → PC adds the NOTES.md
   rule; next task opens with "1 coaching rule loaded" (vs the old
   "No coaching rules yet" screenshot).
3. **Self-learning (the strongest beat — keep this one):**
   `git log --author=ticket-triage --oneline` — the agent's own learning
   history under its own git author; then point at the refund≠wire lesson in
   KNOWLEDGE.md and land the line: "an agent's journal lesson, ratified by a
   human into theme_distinctions.jsonl, took detection from 13/14 to 14/14 —
   the learning loop closing into the deterministic layer."
4. **Skill evolution (humans-only):** agent files a proposal → run the
   digest routine from the Routines UI → reply `approve SCP-xxx` → Triage
   executes; `git log` shows a sophie-authored commit. Cut this beat first
   if over time.

## After the recorded run
Re-export the deliverables so the committed snapshot matches what the camera
saw, then push:
```bash
.venv/bin/python scripts/export_console.py
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless \
  --no-pdf-header-footer --print-to-pdf="$PWD/reports/console-snapshot.pdf" \
  "file://$PWD/reports/console-snapshot.html"
git add reports && git commit -m "post-recording snapshot" && git push
```

## OpenMausBot go/no-go (Day 3 AM, hard 3h)
Entry: all three scenes above already recorded in plain Claude Code.
Go → re-shoot Scene 2 in the war room (4 bots, same SKILL.md files, Liaison
files OPS-101 in chat). No-go at 3h → ship the fallback footage; deliverables
don't mention the war room.
