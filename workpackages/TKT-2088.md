# TKT-2088 — Bluebird Retail

**Status:** resolved (in-system) · resolution_time 52.9h · CSAT 2
**Segment:** SMB · Country: Canada · Product: Payments · MRR: $1,120
**Subject:** Vendor payment returned
**Labeled ops_workflow:** payment_investigation (theme: payment_returned, severity 3, risk_flags: none, needs_human=false)
**Created:** 2026-07-17 10:40 (35 days before today, 2026-08-21)

## Playbook fit
`wire_investigation.md` fits — this is case (c) in the playbook's own
decision list (recall/return), not a statement-reconciliation complaint. No
relabel suggested.

Not part of any active incident/trend cluster — checked `out/clusters.json`
(INC-001, INC-002, TRD-001 memberships), no match.

## Flag: resolved-in-system but low CSAT
Ticket shows `status=resolved` but CSAT is 2 (low). Per the lesson from
TKT-2090 (2026-08-19): resolved-in-system + low CSAT can mean the fix didn't
land with the customer, not that the case is actually closed. There is no
internal resolution-note field in `data/tickets.csv` or
`out/enriched.jsonl` — we cannot tell from ticket data whether the customer
was ever given a return reason, or just got a generic close. Treating this
as needing a fresh look, not as already handled.

## Facts (from ticket text only — none inferred)
| fact | value |
|---|---|
| amount + currency | not stated |
| direction | outbound (customer's own vendor/supplier payment) |
| expected date | not stated |
| counterparty | "a supplier" — no name given |
| business impact | not stated beyond wanting the return reason explained |
| sender confirmation | not applicable — this is a return, not a missing-inbound case |

## Investigation checklist (per wire_investigation playbook, in order)
- [ ] Check the partner-bank portal for the matching outbound payment record to Bluebird Retail's supplier (need amount/date from customer or internal payment log — not in ticket text)
- [ ] Ask the customer for the payment reference / IMAD or UETR if not already provided, since none is in the ticket
- [ ] Trace the reference with the partner bank: confirm return, and pull the return reason code
- [ ] Confirm this is case (c) recall/return per the playbook's classification, and identify the specific return reason (e.g. invalid account, name mismatch, supplier bank rejection)
- [ ] Check whether other Bluebird Retail tickets or similar payment_returned tickets cluster in this window — none currently in `out/clusters.json`, but re-check if new tickets land
- [ ] Log findings here before drafting any further customer update

## Partner/processor inquiry — DRAFT
> Reference: [pending — need customer's payment reference/IMAD/UETR]
> Amount/currency: [pending — not in ticket text, need from customer or internal payment log]
> Expected value date: [pending]
> Beneficiary account: [pending — supplier name/account not given]
> Question: Was this outbound payment received, queued, or returned — and if returned, what is the return reason code?

## Customer update — DRAFT
> Hi [name], thanks for flagging this. We can see a supplier payment was
> returned, but we don't yet have the specific return reason on file, and
> our records show this ticket was marked resolved without that answer
> reaching you — apologies for that gap. To trace it with our partner bank
> we'll need the payment reference or the date/amount if you have it handy.
> Once we have that, we'll check with the bank and get back to you with the
> actual return reason — we'll follow up by [next checkpoint, not yet set].
