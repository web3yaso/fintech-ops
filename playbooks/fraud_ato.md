# Playbook: fraud_ato (STUB)

Extension point — not shipped in P1. Covers: unrecognized users/admins,
unrecognized charges, departed-employee access. Until written, case-worker
produces a best-effort package flagged as unplaybooked.
